# npm 配置文件示例
# 复制此文件为 .npmrc 并填入你的 Token
# 注意：.npmrc 已在 .gitignore 中，不会被提交

# npm 官方源认证 Token
# 从 https://www.npmjs.com/settings/tokens 创建 Automation 类型令牌
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# 或者使用环境变量
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# 如果使用作用域配置（推荐）
# @fsyyft:registry=https://registry.npmjs.org/
